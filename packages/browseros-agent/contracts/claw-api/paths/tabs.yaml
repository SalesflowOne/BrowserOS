get:
  operationId: listTabs
  responses:
    '200':
      description: Current live-tab activity.
      content:
        application/json:
          schema:
            $ref: ../schemas/tabs.yaml#/TabList
    '500':
      $ref: ../responses.yaml#/InternalError
